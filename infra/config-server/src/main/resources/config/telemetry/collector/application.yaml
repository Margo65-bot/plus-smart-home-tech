smart-home-tech:
  kafka:
    bootstrap-servers: localhost:9092
    hub-event-topic: telemetry.hubs.v1
    sensor-event-topic: telemetry.sensors.v1

logging:
  level:
    org.springframework.web: INFO
    org.springframework.validation: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,metrics